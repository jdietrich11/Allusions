import React from "react";
import { View, Text } from "react-native";

import browseStyles from "./browse.styles";

const BrowseScreen: React.FC = (props) => {
  return (
    <View>
      <Text>Page</Text>
    </View>
  );
};

export default BrowseScreen;
