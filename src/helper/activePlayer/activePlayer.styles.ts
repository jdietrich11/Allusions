import { StyleSheet } from "react-native";

const activePlayerStyles = StyleSheet.create({
  playerNameContainer: {
    marginTop: 12,
  },
  playerName: {
    fontSize: 42,
    textAlign: "center",
  },
});

export default activePlayerStyles;
